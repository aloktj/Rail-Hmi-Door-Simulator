<Window x:Class="Hmi.Host.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hmi.Host"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock FontSize="20" FontWeight="Bold" Text="HMI Door Monitor" />
        <ListView x:Name="DoorList" Grid.Row="1" Margin="0,12,0,0">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Door ID" Width="80" DisplayMemberBinding="{Binding DoorId}" />
                    <GridViewColumn Header="State" Width="140" DisplayMemberBinding="{Binding State}" />
                    <GridViewColumn Header="Last Update" Width="180" DisplayMemberBinding="{Binding LastUpdated}" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
